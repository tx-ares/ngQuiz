(function() {

	angular
		.module("pokemonFacts", []); //Note that when creating the module we must pass in a empty placeholder in this case an Array.  This is because if we did not, it would instead SEARCH for a module called "pokemonFacts" instead of creating one.

})();